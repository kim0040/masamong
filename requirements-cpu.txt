# ==============================================================================
# requirements-cpu.txt — 서버 (CPU-only) 환경용 전체 스택
#
# 사용법:
#   pip install -r requirements.txt
#   pip install -r requirements-cpu.txt
#
# 주의:
#   - torch CPU-only 버전을 사용합니다 (GPU 없는 저사양 서버용)
#   - 설치 순서 중요: torch 먼저, 이후 sentence-transformers
# ==============================================================================

# --- PyTorch CPU-only ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# --- RAG / 임베딩 ---
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
numpy>=1.24.0,<3.0

# =============================================================================
# 마사몽 Discord 봇 - 의존성 패키지
# =============================================================================
# 
# 이 파일은 마사몽 봇의 필수 의존성을 정의합니다.
# 저사양 서버 환경을 기본으로 하며, RAG/임베딩 기능은 선택 사항입니다.
#
# 설치: pip install -r requirements.txt
# =============================================================================

# --- 핵심 프레임워크 ---
# Discord API 래퍼 (비동기 처리)
discord.py==2.4.0

# --- 데이터베이스 ---
# SQLite 비동기 드라이버
aiosqlite==0.19.0

# --- HTTP 클라이언트 ---
# 비동기 HTTP 클라이언트 (외부 API 호출용)
aiohttp==3.9.5
# 동기 HTTP 클라이언트 (일부 API 호환성용)
requests==2.31.0

# --- 유틸리티 ---
# 타임존 처리 (KST 변환)
pytz==2024.1
# 환경 변수 로딩 (.env 파일)
python-dotenv==1.0.1

# --- AI/ML ---
# Google Gemini API 클라이언트
google-generativeai==0.6.0

# --- 임베딩/RAG (선택 사항) ---
# NumPy - 벡터 연산 (RAG 기능에 필요)
numpy==1.26.4

# packaging - 버전 비교 유틸리티
packaging==2.7.0

# SentenceTransformers - 로컬 임베딩 모델
# 한국어 semantic search를 위해 필요
# 저사양 서버에서는 AI_MEMORY_ENABLED=false로 설정하여 비활성화 가능
sentence-transformers==2.7.0

# =============================================================================
# 선택적 의존성 (수동 설치)
# =============================================================================
#
# 다음 패키지들은 특정 기능을 위해 필요할 수 있습니다:
#
# --- RAG 고급 기능 ---
# torch>=2.0.0              # GPU 가속 (CUDA 필요)
# transformers>=4.30.0      # Cross-Encoder 리랭킹
#
# --- 개발 도구 ---
# pytest>=7.4.0             # 테스트 실행
# pytest-cov>=4.1.0         # 코드 커버리지
# pytest-asyncio>=0.21.0    # 비동기 테스트
# black>=23.0.0             # 코드 포매팅
# flake8>=6.0.0             # 린터
# mypy>=1.4.0               # 타입 체크
#
# --- 프로덕션 유틸리티 ---
# pyyaml>=6.0               # YAML 설정 파일 지원 (prompts.yaml)
#
# =============================================================================

# 설치 예시:
#
# # 최소 설치 (RAG 제외)
# pip install discord.py aiosqlite aiohttp requests pytz python-dotenv google-generativeai packaging
#
# # 전체 설치 (RAG 포함)
# pip install -r requirements.txt
#
# # 개발 환경
# pip install -r requirements.txt pytest pytest-cov pytest-asyncio black flake8
#
# =============================================================================

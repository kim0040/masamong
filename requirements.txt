# --- Discord 봇 핵심 라이브러리 ---
discord.py>=2.3.0

# --- AI 및 데이터 처리 ---
# Google Gemini AI 라이브러리
google-generativeai>=0.5.0

# --- RAG 및 벡터 임베딩 ---
sentence-transformers
numpy
# 저사양 서버를 위해 CPU 버전의 torch를 명시
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# 비동기 SQLite 드라이버 (1단계부터 사용)
aiosqlite>=0.19.0

# --- 한국어 자연어 처리 (NLP) ---
# (konlpy, mecab-ko 등은 현재 버전에서 사용되지 않음)

# --- 기타 유틸리티 ---
# .env 파일을 읽기 위한 라이브러리
python-dotenv>=1.0.0

# 시간대 처리를 위한 라이브러리
pytz

# HTTP 요청을 위한 라이브러리 (기상청 API 등)
requests
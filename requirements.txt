# ==============================================================================
# requirements.txt — 마사몽 공통 패키지 (RAG 제외)
# 서버(CPU-only) 및 로컬(GPU) 환경 공통 기본 패키지입니다.
# RAG/임베딩 기능이 필요한 경우:
#   서버: pip install -r requirements-cpu.txt
#   로컬: pip install -r requirements-gpu.txt
# ==============================================================================

# --- Discord ---
discord.py>=2.7.1

# --- 데이터베이스 ---
aiosqlite>=0.20.0
PyMySQL>=1.1.0

# --- 환경변수 / 설정 ---
python-dotenv>=1.0.0

# --- Google AI / Gemini ---
google-generativeai>=0.8.0
google-genai>=1.0.0

# --- LLM API (OpenAI-compatible: CometAPI) ---
openai>=2.20.0

# --- HTTP/네트워크 ---
requests>=2.31.0
aiohttp>=3.9.0
beautifulsoup4>=4.12,<5
soupsieve>=2.6,<3
olefile>=0.47,<1
pypdf>=5,<7

# --- 주식 / 금융 ---
yfinance>=0.2.30

# --- 유틸리티 ---
korean-lunar-calendar>=0.3.0
psutil>=5.9.0
pytz>=2023.3
ephem>=4.1.0
matplotlib>=3.9.0
seaborn>=0.13.0

# --- 뉴스 RAG 파이프라인 (DuckDuckGo, 무료, API 키 불필요) ---
ddgs>=6.0.0
trafilatura>=1.6.0
newspaper4k>=0.9.0
nltk>=3.8.0
lxml_html_clean>=0.1.0

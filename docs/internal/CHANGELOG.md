# 내부 변경 이력

이 파일은 릴리스 단위의 주요 변경을 Git과 함께 추적하는 내부 기록입니다. 사용자에게
보이는 현재 기능은 [문서 허브](../README.md)와 기능 기준 문서를 우선합니다. 작업 중간
메모나 계획은 이력에 섞지 않고, 재현 가능한 판단 근거만 남깁니다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/)를 참고하며, 버전 번호는
[Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

## [Unreleased]

### 문서

- 공개 README를 프로젝트 소개와 시작 지점으로 정리하고, 문서 허브를 추가
- RAG 실측·개선 기록·인덱스 이행 기준·변경 이력을 `docs/internal/`으로 분리
- Discord 기본 진행 표시의 동작·상한·비용 경계를 사용자·설정·아키텍처 문서에 반영

### 추가

- Masamo/일반 프로필의 봇 ID, DB, 경로, 관리자, scheduler 소유권을 기동 전에
  검증하는 fail-closed 인스턴스 분리
- 의미 라우팅, 선택적 기억 회수, 동일 호출 내 컨텍스트 digest로 구성한 대화 하네스
- 학교 공지 자연어 등록·확인·초기 수집·05시 순차 수집·사용자별 09시 기본 알림
- 20개 대학 편입 공지 DM 구독과 공식 원문 링크
- 기능별 개인정보 동의 버튼, 철회 이력, DM 전용 개인 기능
- 기상청 지진 1분 감시와 연속 사건 메시지 수정, 특보·태풍·예보 확장
- CometAPI Gemini native 이미지 생성과 대화 기억 기반 프롬프트
- 전역/기능/서버/사용자/DM별 LLM·이미지·웹 검색 사용량 예약과 circuit breaker

### 변경

- 텍스트 라우팅·최종 답변·학교 공지 분석을 OpenRouter
  `openai/gpt-5.6-luna`로 전환하고 OpenAI 공급자만 허용. 라우팅은 `low`,
  최종 답변은 같은 라우팅 결과의 `none/low/high`를 사용하며 낮은 단계 실패 뒤
  재호출하지 않음. CometAPI 키와 Gemini native 경로는 이미지 생성 전용으로 유지
- 운영 RAG를 스코프가 분리된 의미 임베딩 및 TiDB 벡터 검색으로 정리
- 저사양 운영 프로필에서 BM25/FTS5 생성·검색·자동 재구축을 명시적으로 금지
- 최근 원문, 관련성 gate, 최대 기억 블록으로 불필요한 기억 주입을 차단
- 과거 공동 대화 참조와 현재 외부 사실 검증을 별도 라우팅 신호로 분리해, 필요한
  요청에서만 서버 기억과 한 번의 웹 검색을 함께 사용
- 전송 완료된 봇 답변을 서버·DM별 `assistant_response` 기억으로 추가 저장하고,
  명시적인 자기 선택은 `assistant_commitment`로 구분. LLM 요약 없이 로컬 임베딩
  한 번만 사용하며 기존 기억 행은 변경하지 않음
- 명시적 회상에서 Discord 최고점이 Kakao 후보를 굶기지 않도록 절대 의미 관문과
  어휘 겹침을 통과한 다른 출처 후보를 최대 한 건 보존
- none/low 답변 출력 상한과 routing 전용 25초 timeout을 분리하고 high 요청은 기존
  출력 예산을 유지
- 동시 Discord 질문을 유한 FIFO에 접수하고 저사양 설정의 worker 수만큼 처리하도록
  변경. 대기 중에는 외부 API를 호출하지 않으며 재시작 시 미처리 항목을 재실행하지 않음
- 최신·금융·지역 사실 질문은 성공한 외부 근거가 없으면 답을 만들지 않도록 변경
- Discord 메뉴를 기능 그룹으로 정리하고 개인 메뉴와 공개 출력 경계를 분리
- 편입 지원 대학의 공식 공지 링크를 특정 대학 전용 버튼 없이 하나의 동일한 선택
  메뉴로 제공하고, 자동 접근 불가 출처는 직접 확인으로 명확히 구분
- 학교·편입·운세 알림의 재시작 중복 전송 방지와 실패 항목의 제한적 재시도 강화

### 수정

- 존재하지 않는 주식 티커 두 건이 전체 주식 도구 회로를 열어 정상 종목까지 잠시
  차단하던 분류와, 현재가 도구가 차트·ADR·환율 요청을 억지로 처리하던 경로
- 환율 환산의 결정적 곱셈 결과가 검색 원문에 그대로 없다는 이유로 근거 없는 금융
  수치로 차단되던 검증 오탐
- 동시 이미지 요청의 5초 대기 실패와 HTTP 200 빈 이미지 응답의 진단 정보 부족
- 첫 봇 답변 임베딩 모델 로드가 전송 뒤 대화 worker 종료를 늦추던 시작 시점
- 단일 TiDB 연결에서 서로 다른 비동기 작업의 쓰기가 한 commit/rollback에 섞일 수
  있던 트랜잭션 경계
- Discord 버튼 acknowledgement 지연, 이미지 중복 생성, 출처 반응 토글 동작
- 서버 페르소나가 다른 서버 또는 공통 재난 알림에 섞일 수 있던 스코프 경계
- KRX TLS/API 키 경로, yfinance 무제한 대기, 취소된 도구 호출의 회로 상태 누수
- 실패한 검색 문자열이 성공 근거처럼 최종 답변에 전달되던 경로
- 웹검색·날씨 등 도구가 정상 종료되어도 성공·빈 결과·실패를 구분할 수 없던 운영 로그
- AI 요청 종료·단계·전체/LLM/도구 소요 시간이 없어 느린 구간과 중단 지점을 찾기
  어려웠던 운영 로그
- 1분 지진 정상 조회가 매번 INFO로 쌓여 중요한 오류와 요청 흐름을 가리던 로그 노이즈
- 장시간 실행 중 cooldown·emoji 캐시가 무제한 커질 수 있던 경로
- 비용 발생 smoke 스크립트가 도움말이나 무인 실행만으로 provider를 호출하던 경로
- 과거 대화를 전제로 한 공개 서비스 장애 질문이 기억과 웹을 모두 생략하고 모델
  내장 지식만으로 원인을 단정하던 경로
- Kakao 방 이름을 저장소 origin으로 사용해 실제 검색 결과가 `UNKNOWN`으로 집계되던
  진단 로그와, 단순 상대 평가·동의 표현이 사용자 선호로 분류되던 기억 정제
- 봇의 과거 선택이 장기 기억에 저장되지 않아 최근 Discord 원문 범위를 벗어나면
  이전 답변과 반대로 말하던 자기 일관성 결함

### 검증

- 오프라인 기능·회귀 테스트, 명령어 표면 검사, 문법 컴파일, 추적 파일 비밀값 검사
- 운영 DB를 삭제·수정하지 않는 stale-read 상태/용량/기억 품질 감사
- 실제 Discord 의미 라우팅 smoke와 배포 후 systemd·timer·오류 로그 검증

## [2.0.0] - 2026-01-19

### 추가 (Added)

#### 문서
- 📚 **README.md 대폭 개선**
  - 3개의 Mermaid 아키텍처 다이어그램 추가
  - 목차 및 배지 추가
  - 50개 이상의 환경 변수 전체 문서화
  - 문제 해결 가이드 섹션 추가
  - Discord 사용 가이드 확장
  - RAG 시스템 상세 설명
  - Systemd 서비스 설정 예시
  - 성능 최적화 팁 추가

- 📖 **ARCHITECTURE.md** - 기술 아키텍처 문서
  - 2단계 에이전트 패턴 설명
  - 하이브리드 RAG 알고리즘 상세 설명
  - 데이터 레이어 구조
  - 성능 최적화 전략
  - 보안 고려사항
  - 모니터링 및 관찰성
  - 배포 가이드라인

- 🤝 **CONTRIBUTING.md** - 기여 가이드
  - 개발 환경 설정 방법
  - 코드 스타일 가이드
  - 새 Cog 추가 방법
  - 테스트 작성 가이드
  - Pull Request 절차
  - 커밋 메시지 컨벤션

- 📋 **CHANGELOG.md** - 변경 이력 관리

#### 코드
- ✨ **main.py**: 버전 정보 추가 (`__version__ = "2.0.0"`)
- 📊 **main.py**: 시작 시 시스템 정보 로깅
  - Python 버전
  - Discord.py 버전
  - 작업 디렉터리

### 변경 (Changed)

#### 문서
- 📝 **requirements.txt** - 완전 재구성
  - 모든 패키지 버전 고정 (재현성 확보)
  - 상세한 주석 추가
  - 섹션별 구조화 (핵심, DB, HTTP, AI/ML 등)
  - 선택적 의존성 안내 추가
  - 설치 예시 추가

### 최적화 (Optimized)

#### 성능
- ⚡ **utils/hybrid_search.py**: Regex 패턴 캐싱
  - `_URL_PATTERN`, `_WHITESPACE_PATTERN` 모듈 레벨 컴파일
  - 성능 향상: ~20-30% (정규식 중심 작업 시)

- 🚀 **utils/hybrid_search.py**: 중복 제거 알고리즘 개선
  - 문자열 연결 대신 튜플 + 해시 사용
  - `set[str]`에서 `dict[tuple, bool]`로 변경
  - 성능 향상: ~10-15% (대량 중복 데이터 시)

### 수정 (Fixed)

- 🔧 **.gitignore**: `emb/` 디렉터리 추가하여 임베딩 파일 제외

---

## [1.x.x] - 이전 버전

이전 버전의 변경사항은 Git 커밋 히스토리를 참조하세요:
```bash
git log --oneline
```

---

## 버전 관리 규칙

### 버전 번호: MAJOR.MINOR.PATCH

- **MAJOR**: 호환되지 않는 API 변경
- **MINOR**: 하위 호환되는 기능 추가
- **PATCH**: 하위 호환되는 버그 수정

### 변경 유형

- `Added`: 새로운 기능
- `Changed`: 기존 기능의 변경
- `Deprecated`: 곧 제거될 기능
- `Removed`: 제거된 기능
- `Fixed`: 버그 수정
- `Security`: 보안 관련 수정
- `Optimized`: 성능 최적화

---

[2.0.0]: https://github.com/kim0040/masamong/compare/v1.0.0...v2.0.0
